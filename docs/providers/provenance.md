# Milestone 0 provenance and attribution register

Validation date: 2026-09-22. “Live checked” means only response metadata and schema shape were inspected; no provider secret or unapproved full response is committed.

| Provider/dataset | URL | Transport | Observed result | Required attribution / terms check |
|---|---|---|---|---|
| IPMA warnings | https://api.ipma.pt/open-data/forecast/warnings/warnings_www.json | Browser-direct | HTTP 200; 215 records; wildcard CORS | Cite IPMA and link source; re-check API conditions before public release |
| IPMA seismic 3/7 | https://api.ipma.pt/open-data/observation/seismic/ | Browser-direct | HTTP 200; 76 / 182 records | Cite IPMA and link source; distinguish observed events from forecasts |
| Portugal–Spain land border | https://github.com/nvkelso/natural-earth-vector/blob/master/geojson/ne_10m_admin_0_boundary_lines_land.geojson | Bundled static asset | High-resolution visual line; only the shared Portugal–Spain land boundary is included | Display aid, not an authoritative administrative dataset |
| IPMA area metadata | https://api.ipma.pt/open-data/distrits-islands.json | Browser-direct | HTTP 200; area/capital metadata | Source citation; not a warning polygon dataset |
| OpenSky | https://opensky-network.org/api/states/all | Broker | OAuth 200; wide box 145 states; quota headers observed | OpenSky citation and non-commercial terms; do not claim exhaustive coverage |
| Fogos.pt | https://api.fogos.pt/v2/incidents/active | Broker | 2026-09-22 request was HTTP 429 / 1015; a 2026-09-23 request reached the endpoint and returned 39 active incidents, but endpoint scope/quota approval remains unconfirmed | Visible “Fonte: Fogos.pt” and link to https://fogos.pt; keep further validation blocked pending written API authorization/quota confirmation |
| NASA FIRMS | https://firms.modaps.eosdis.nasa.gov/api/area/ | Broker | NOAA-20 NRT CSV schema verified; production adapter queries Portugal regions and filters trailing 24 hours | Visible NASA FIRMS source link; review product citation requirements before public release |
| Instituto Hidrográfico tide gauges (L1) | https://ogcapi.hidrografico.pt/collections/tide_obs_nrt/instances/l1/locations?f=json&limit=100 | Browser-direct | 2026-09-24 HTTP 200; 35 stations; wildcard CORS; one minute source cadence observed. `id`, Point `[lon,lat]`, `title`, `last_date_time` UTC, `last_sea_surface_height` metres above ZH. No `Cache-Control` / `ETag` observed. Bounded 11-minute timeseries checks for `152-303` and `1001-307` returned 12 samples each through 21:36Z, last heights 1.562 m and 1.698 m, QC flags 0. L1 itself remains explicitly without QC; flag values are retained only as validation evidence, not implied on the map. | Attribute Instituto Hidrográfico; link collection metadata and user guide; CC BY-NC 4.0 and DOI requirements apply. Commercial use needs separate permission. Values are raw L1 NRT observations relative to ZH (EPSG:10349), not astronomical predictions, mean-sea-level heights or flood warnings. |
| NetMadeira public camera locations | https://www.netmadeira.com/webcams-madeira/ | Curated 28-camera catalogue; each marker links to its individual camera page; no player rendered | On 2026-09-23, the official list exposed 28 named cameras. Published `/webcams/show/netmadeira/<slug>` iframe pages return `disable.jpg`; individual camera pages expose time-varying signed still-image URLs that return JPEG bytes while declaring `Content-Type: text/html`. Camera mounts remain approximate. | Link to each official camera page. Do not render the disabled iframe or scrape/relay/store signed frames. A broker could technically normalize image content and refresh signed URLs, but requires a supported reuse contract and explicit transport review. Recheck terms and availability before release. |
| Porto de Lisboa Tejo Live | https://www.portodelisboa.pt/tejo-live | Host `www.portodelisboa.pt`; two curated points; official YouTube player URLs embedded on selection; catalogue refresh daily | Official page publishes YouTube embed IDs `Asr_I2BMS3c` (Cacilhas) and `FE-JfjvXFEU` (VTS/Algés). Coordinates remain approximate. Embed pages returned HTTP 200 on 2026-09-23; actual live playback should be confirmed in app. | Attribute Porto de Lisboa / APL and link its official page. No stream extraction, proxy, or recording. Page reports approximately 30-second delay; no exact capture time claim. |
| Clube Naval de Santa Maria webcam | https://www.cnsantamaria.pt/broadcast-live/ | Host `www.cnsantamaria.pt`; one curated point; official club page links player page `https://cnsm.olho.mariense.pt/index.html`; daily catalogue refresh | The club page names “Câmara Clube Naval”; its location page says the club is at Marina de Vila do Porto and publishes GPS 36°56′47.5″N 25°08′51.5″W. Marker uses the club coordinate as approximate location. Player page identifies HLS playlist `https://cnsm.olho.mariense.pt/cam/camera.m3u8`; playlist and referenced MPEG-TS segment returned HTTP 200 on 2026-09-23. | Attribute Clube Naval de Santa Maria and link its official camera page. Embed only the linked player page; no raw playlist relay or recording. Feed behavior and additional reuse terms remain unverified. |
| MEO Beachcam camera index | https://beachcam.meo.pt/livecams/ | Curated 185-camera catalogue from the official index; each marker links to its individual source page; no player rendered | On 2026-09-24, the official index exposed 185 unique camera names, slugs, and `data-lat`/`data-lon` coordinates across mainland Portugal, Madeira and the Açores. The Monte Verde sample remains the only individually checked embed; its HLS returned HTTP 403 and its page blocked framing on 2026-09-23. | Attribute MEO Beachcam and link each official camera page. Keep availability unverified individually; do not generalize Monte Verde's 403 to the other pages. |
| MeteoEstrela weather webcams | https://www.meteoestrela.pt/web-tv-nowcasting/ | Ten official image views at five grouped map locations; direct JPEG preview while selected; refresh every 60 seconds | Page names Torre (2), Estância (4), Penhas da Saúde (2), Vale do Rossim and Covilhã; states that views are meteorological, not surveillance, and are not recorded. `cam_100.jpg` returned HTTP 200 with `Content-Type: image/jpeg` on 2026-09-24. Map coordinates are approximate site locations. | Attribute MeteoEstrela and link to each corresponding site page. Load the listed public image URL only for a selected camera, refresh at the stated 60-second cadence, pause when hidden, and do not store frames. |
| Madeira-Web public camera pages | https://www.madeira-web.com/es/noticias/webcams-isla-madeira.html | Curated 30 fixed camera locations from linked operator pages; four selected views use published YouTube iframe URLs, remaining pages are link-only | Operator index describes about 30 live cameras. Funchal Marina 1 page publishes coordinates; other marker positions are approximate town/site coordinates. Individual pages for Marina 1, Lido pools, Caniçal Whale Museum, and Machico harbor linked YouTube iframe URLs on 2026-09-24. Operator states live footage is not recorded or stored. | Attribute Madeira-Web; load only the published iframe URLs for selected cameras, never extract or relay media. Keep direct page links for all entries. Other 26 player embeds have not been verified. |
| Via Verde / Brisa highway cameras | https://www.viaverde.pt/Ferramentas/informacao-de-transito | Dynamic catalogue fetched server-side through `/DesktopModules/Traffic/Handlers/Api.ashx?action=cameras&lang=pt-PT`; 100 camera records across A1–A4. Broker discovers the current DNN `TabId`, obtains page cookies, and makes the same-origin catalogue request; browser receives only IDs, names, roads and WGS84 coordinates. | Catalogue returned 100 unique located cameras during the 2026-09-24 review. The official page is same-origin framed. Twelve sampled S3 image objects reported `Last-Modified` on 2026-09-13, so snapshot freshness is unverified/stale; no capture timestamp is exposed. | Link markers to the official traffic page. Do not render or proxy snapshot images as live until current capture times and reuse are established. Broker limits response sizes/time, pins hosts and caches catalogue metadata for six hours. |
| VR1 Madeira / AIGIH road cameras | https://www.vr1madeira.pt/map | Operator JSON catalogue at `https://www.vr1madeira.pt/assets/json/cctvs.json`; 71 records with names and coordinates (64 VR1, 7 DRE), captured into the app catalogue on 2026-09-24. | Operator identifies new concession holder AIGIH and exposes a current map with camera layer. Sample `cctv.php?image=001` and `cctv_dre.php?image=129` requests returned HTTP 403 on 2026-09-24, including same-origin request headers. | Attribute VR1 Madeira / AIGIH Madeira, ACE and link to its map. Markers use published coordinates, but do not render an image/player unless the image handler becomes publicly available and reuse is clear. |
| Município do Funchal webcams | https://services.funchal.pt/webcams/ | Official page lists Baía do Funchal (`view=1`), Torre (`view=3`) and Parque Ecológico (`view=5`); page reloads every 60 seconds. Approximate coordinates assigned from the named locations. | All three `image.php?view=` endpoints returned the same `image/svg+xml` fallback (98,455 bytes) on 2026-09-24 instead of distinct camera frames. | Attribute Município do Funchal and link to its page. Do not render SVG fallback as a camera view; recheck endpoint before enabling media. |
| APA / QualAr portal measurements | https://qualar.apambiente.pt/downloads | The reviewed public page exposes station-oriented downloads but no documented machine measurement contract. Portal internal calls are not used. | Not used as a runtime feed. | Direct QualAr machine access remains unconfirmed; the implemented map identifies its EEA source separately. |
| EEA air-quality measurements reported by Portugal (E2a) | https://www.eea.europa.eu/en/datahub/datahubitem-view/778ef9f5-6293-4846-badd-56a29c70880d | Official API `https://eeadmz1-downloads-api-appservice.azurewebsites.net/ParquetFile/dynamic`, OpenAPI at `/swagger/v1/swagger.json`. Request uses dataset `1`, source `E2a`, country PT, six pollutant vocabulary URIs, hourly aggregation, and a 48-hour bound. A live broker fetch on 2026-09-24 returned 9,032 Parquet rows for 74 inventory stations. The parser retained 174 latest valid station/pollutant records at 55 stations and rejected 1,143 rows, principally EEA validity values `-1`/`-99` and unmatched/invalid rows. Latest accepted `Start` was 18:00Z; fetched at 20:33:44Z. ResultTime and Verification/Validity are preserved. The EEA catalogue declares CC BY 4.0; E2a is up-to-date data reported by countries, unverified. | Brokered through `/api/providers/qualar/observations`; 15-minute cache, bounded request and last-good cache on errors/429. Station metadata is joined by `AirQualityStationEoICode` against EEA layer 0. UI says EEA data reported by Portugal (E2a), shows raw validity/verification interpretations, units, averaging periods, observation/result times, and station class. | Attribution: European Environment Agency (EEA), data reported by Portugal, CC BY 4.0. E2a is provisional/unverified and can be revised; do not describe values as validated or derive a health index. |
| ViaExpresso Madeira cameras (reviewed, excluded) | https://www.viaexpresso.com/ | Public HLS camera paths discovered in a third-party catalogue; no entries integrated | Three sample official-host HLS playlists and a segment returned HTTP 200 on 2026-09-24. ViaExpresso's current official terms say copying site information/images or creating links to `viaexpresso.com` requires prior written consent. | Do not add camera links or media until written consent is obtained. |
| CIVISA Azores volcanic webcams (reviewed, not integrated) | https://www.civisa.pt/camaras-web-em-tempo-real-2 | Public page only; no camera URLs or media requested | CIVISA describes live volcano views but the reviewed page does not identify individual camera IDs or locations. Not enough evidence for mapped markers. | Link-only candidate review; no media reuse or attribution claim made by the app. Revisit if a located catalogue/access record becomes available. |

The repository's synthetic fixtures are examples of documented shapes and parser edge cases, not a redistribution of provider snapshots. Any future committed real fixture requires a provider-term review first.

## Live validation follow-up — 2026-09-23

Controlled local browser window: 11:17–11:27 WEST, app origin
`http://localhost:4174`, broker `http://127.0.0.1:8787`, with
`BROKER_ALLOWED_ORIGIN=http://localhost:4174`. All five layers were enabled.
The UI showed provider links and explicit partial/live states. After stopping
the broker, the three brokered layers changed to `FALHA DE REDE · 0 …`; both
IPMA layers remained available and warnings refreshed directly. This verifies
the separation and failure labels, not every DevTools request detail.

| Provider | Current observed result | Freshness, parsing, and interface | Attribution and remaining uncertainty |
|---|---|---|---|
| IPMA seismicity | Both area feeds HTTP 200. Area 3: 70 accepted / 0 rejected; area 7: 191 / 5; UI: 261 events, `PARCIAL`. | `updateDate` 2026-09-23 08:36:01Z; envelope has 266 rows. Browser-direct area 3/7 JSON. | IPMA link visible; event age is separate from feed update time. Partial rows were rejected by parser validation. Reuse conditions still need review before public release. |
| IPMA warnings | Feed and area metadata HTTP 200. 213 source rows; 11 displayed (9 active, 2 upcoming); 2 rejected; UI: `PARCIAL`. | Browser-direct JSON plus `distrits-islands.json`; fetched 11:27 WEST. Green/no-warning and expired rows are intentionally omitted. | IPMA link and representative-point caveat visible. Selected warning detail showed phenomenon, severity, validity, area, and end time; no polygon or exact affected location is claimed. |
| OpenSky | OAuth and bounded wide `/api/states/all` HTTP 200; recent snapshots had 126, then 139/140/140, and browser UI 143 states. The UI showed `DISPONÍVEL` (no parser rejects). Three controlled calls each took 172–206 ms and returned 17,949–18,137 bytes; the earlier 126-state response was 16,356 bytes. A coastal/Atlantic-edge query at 35.7–37.0°N, 10.0–6.0°W returned 13 positioned vectors in 348 ms. | Wide snapshot times 10:26:56Z, 10:36:28Z, 10:38:11Z, 10:40:08Z; browser snapshot at 11:46 WEST; coastal sample at 10:58:19Z. Three measured wide calls consumed 3 credits each, latest then 3,967 remaining; the restored broker later exposed 3,964 to the exact allowed origin; coastal response exposed 3,957. The latest 7-credit balance change cannot be attributed solely to the documented 1-credit coastal query. Official OpenSky docs list 4,000 credits/day for standard users and 1 credit for boxes ≤25 sq°. The 90-second wide-box plan projects 2,880/day, below the standard allowance if other shared `/states/*` use leaves capacity. Account tier and concurrent/shared use remain unconfirmed. | OpenSky link and non-exhaustive coverage note visible. Three anonymized records had fresh fixes and plausible movement. Two selected details showed missing altitude/speed/heading as `—` while retaining callsign, origin, source, and observation time. Do not infer empty-area coverage from a zero count. |
| Fogos.pt | Brokered layer returned 39 incidents; `DISPONÍVEL` indicates 39 accepted and no parser rejects, then `FALHA DE REDE · 0 INCIDENTES` with broker stopped. | Last visible source time 11:25 WEST. Configured identifying User-Agent was used by the broker; cache TTL is 3 minutes. No current quota/cooldown header was visible on success. | Fogos.pt link, status/resource legend, and error-vs-zero distinction visible. A successful response proves reachability with this key, not provider-approved endpoint scope or quota. Written provider confirmation remains a release blocker; do not scrape or bypass rate limits. |
| NASA FIRMS | Broker HTTP 200, `live`, sensor `VIIRS_NOAA20_NRT`, no failed regions. Mainland: 57 accepted / 0 rejected; Madeira and Açores: valid empty CSVs, 0 / 0. Browser showed 57 detections. | Regional requests fetched 10:17:32–10:17:33Z; newest acquisition 03:28Z (~6h50m lag), oldest 2026-09-22 13:27Z. Broker cache 30 minutes. A later controlled refresh started from a 0-counter baseline and used 12 of 5,000 transactions for the three-region request. The official Europe WFS map layer returned 69 rows in its own 24-hour mainland sample; one matched the selected UI record at 03:28Z UTC after timezone conversion (nominal confidence, brightness 347.2 K, FRP 9.8 MW). NASA says its WFS data updates every 15 minutes, so counts need not match a different fetch time. | NASA FIRMS link and thermal-anomaly-not-confirmed-fire caveat visible. Selected detail showed satellite, instrument, confidence, FRP, brightness temperature, region, coordinates, and acquisition time. Empty regions are valid responses; bounding boxes include non-Portuguese areas. Redistribution and product-specific citation still need review before public release. |

The broker-only keys and OpenSky credential-file setting were removed from the
ignored `.env.local`; only the Cesium ion token remains there, while the
credentials remain in `.env.broker.local` / `.secrets/`. The client bundle scan
passed (382 output files), and CORS returned the exact configured local origin.

Validation-pass exit criteria are met for IPMA, OpenSky, and FIRMS, with the
Fogos.pt endpoint explicitly blocked from further live validation pending
written confirmation of key scope and quota. Its successful reachability
response is not provider approval; selected incident comparison was therefore
not attempted. Reuse/citation terms still require a deployment review, which
is separate from this validation pass. No live response was committed as a
fixture. The OpenSky quota-header and 429/stale-cooldown fixes passed focused
broker tests, typecheck, client secret scan, and a live response/CORS recheck. The current
local regression run passed 80 unit tests and 16 browser tests across
Chromium, Firefox, WebKit, and mobile WebKit; the first hosted run still has
Chromium/Firefox failures that were not reproduced locally.
